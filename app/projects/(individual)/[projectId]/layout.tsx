import type React from 'react'

interface ProjectIdUIProps {
  children: React.ReactNode
}

const ProjectIdUI: React.FC<ProjectIdUIProps> = (props) => {
  return props.children
}

export default ProjectIdUI

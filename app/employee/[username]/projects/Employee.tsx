const Employee: React.FC<{ username: string }> = (props) => {
//   const data = await getEmployee(props.username)

  //   const employee = data?.data
  //   const supervisor = employee?.supervisor

  return (
    <>Employee!!!</>
  )
}

export default Employee
